import * as React from 'react';
export declare type TableBodyProps = {
    className?: string;
    style?: React.CSSProperties;
    children?: React.ReactNode | React.ReactNodeArray;
    innerRef?: any;
};
export default function Index(props: TableBodyProps): JSX.Element;
