import * as React from 'react';
export declare type LoadingSpinnerProps = {
    className?: string;
    style?: React.CSSProperties;
    size?: number;
    speed?: 'slow' | 'default' | 'fast';
};
export default function Index(props: LoadingSpinnerProps): JSX.Element;
