import * as React from 'react';
export declare type ShowProps = {
    show: boolean;
    children?: React.ReactNode | React.ReactNodeArray;
};
export default function Index(props: ShowProps): JSX.Element;
