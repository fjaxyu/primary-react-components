import * as React from 'react';
declare const Index: (props: MediaImageProps) => JSX.Element;
export default Index;
export declare type MediaImageProps = {
    className?: string;
    style?: React.CSSProperties;
    children?: React.ReactNode;
    src: string;
    position?: string;
    order?: string;
};
