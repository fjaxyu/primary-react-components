import * as React from 'react';
export declare type Index = {
    className?: string;
    style?: React.CSSProperties;
    children?: React.ReactNode | React.ReactNodeArray;
    innerRef?: any;
};
export default function MediaBody(props: Index): JSX.Element;
