import * as React from 'react';
export declare type PlaceholderProps = {
    className?: string;
    style?: React.CSSProperties;
    children?: React.ReactNode;
    title?: boolean;
    block?: boolean;
    inline?: boolean;
};
export default function Index(props: PlaceholderProps): JSX.Element;
